"use strict";

{
	const PLUGIN_CLASS = SDK.Plugins.wastrel_switchcase;

	PLUGIN_CLASS.Type = class wastrel_switchcaseType extends SDK.ITypeBase
	{
		constructor(sdkPlugin, iObjectType)
		{
			super(sdkPlugin, iObjectType);
		}
	};
}
